http
  head
    include includes/head.pug
    title #{project} - Languages
  body
    div.container.mt-2
      header 
        h2 #{project} Languages
      section
        div.list-group
          each language in languages
            a.list-group-item.list-group-item-action(href=language) #{language}